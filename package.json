{
  "name": "focusflow-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "nodemailer": "^6.9.14"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
